{
  "name": "bhmb-starter",
  "version": "1.0.0",
  "description": "A simple starter project to make it simpler to create extensions for the Blockheads MessageBot",
  "main": "./build/bundle.js",
  "scripts": {
    "build": "webpack --env --progress --config webpack.config.js",
    "build:watch": "npm run build -- --watch",
    "build:production": "npm run build -- -p",
    "build:production:watch": "npm run build:production -- --watch"
  },
  "author": "Bibliofile",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.24.1",
    "babel-loader": "^7.0.0",
    "babel-preset-env": "^1.5.1",
    "blockheads-messagebot": "git://github.com/Bibliofile/Blockheads-MessageBot#master",
    "raw-loader": "^0.5.1",
    "ts-loader": "^2.1.0",
    "tslint": "^5.3.2",
    "typescript": "^2.3.3",
    "webpack": "^2.6.1"
  }
}
